name: SL Engine

on:
  schedule:
    - cron: '30 14 * * 1-5'   # 8:00 PM IST (Mon–Fri)

  workflow_dispatch:

  push:
    branches:
      - main
    paths:
      - 'Webhook-app/**'
      - '.github/workflows/**'

permissions:
  contents: write

jobs:
  run-sl:
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest

    env:
      DHAN_CLIENT_ID: ${{ secrets.DHAN_CLIENT_ID }}
      DHAN_PIN: ${{ secrets.DHAN_PIN }}
      DHAN_TOTP_SECRET: ${{ secrets.DHAN_TOTP_SECRET }}
      TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      # NOTE: Removed automatic creation of trades.db so repository can avoid a committed DB file

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pyotp yfinance pandas

      # ✅ FIXED (separate step)
      - name: Set remote with token
        run: |
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}

      # ✅ FIXED (separate step)
      - name: Debug env
        run: |
          echo "TOTP present: $([[ -n \"$DHAN_TOTP_SECRET\" ]] && echo YES || echo NO)"
          echo "PIN present: $([[ -n \"$DHAN_PIN\" ]] && echo YES || echo NO)"

      - name: Run SL Engine
        run: python Webhook-app/sl_engine.py

      - name: Push results (no DB commit)
        run: |
          git config user.name "bot"
          git config user.email "bot@github.com"

          git pull origin main --rebase || true

          # Do not add or commit trades.db anymore
